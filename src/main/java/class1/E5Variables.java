package class1;

public class E5Variables {
    public static void main(String[] args) {
        //byte->data type which tells the computer that type of information we are going to store
        //age->variable/container/box/label
        /*
        = -> sign is used to store something in a variable /container/box/label
         16->the value that we want to store in ege
        */
        byte age=16;
        System.out.println(age);
    }
}
